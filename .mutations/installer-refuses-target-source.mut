# A build directory is not an installation (ADR-0004's eight-day outage):
# dropping the refusal lets an install source binaries out of target/.
file: swift/Sources/BansheeCore/DaemonInstaller.swift
test: DaemonInstallerTests
---OLD---
            if source.pathComponents.contains("target") {
                return .failure(.buildDirectorySource(source.path))
            }
---NEW---
